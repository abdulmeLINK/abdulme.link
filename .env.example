APP_NAME=abdulmeLINK
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://abdulme.link
CIPHER=aes-256-cbc

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=laravel
DB_USERNAME=root
DB_PASSWORD=

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

TRUSTED_PROXIES=*
HTTPS=on
SESSION_SECURE_COOKIE=true

# =====================================================
# Supabase Configuration (REQUIRED for production)
# =====================================================

# Supabase project URL (from your Supabase dashboard → Project Settings → API)
# Example: https://abcdefghijklmnop.supabase.co
SUPABASE_URL=https://abcdefghijklmnop.supabase.co

# Supabase Anonymous/Public Key (for client-side API calls)
# Get from: Supabase Dashboard → Project Settings → API → anon/public
SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (for server-side operations - KEEP SECRET!)
# Get from: Supabase Dashboard → Project Settings → API → service_role
SUPABASE_SERVICE_KEY=your-service-role-key-here

# =====================================================
# Supabase Storage Configuration
# =====================================================

# Enable Supabase Storage for images (wallpapers, projects, assets)
# When true: Uses Supabase CDN as primary, local files as fallback
# When false: Uses local files only (public/images/)
USE_SUPABASE_STORAGE=true

# Storage bucket for JSON data (portfolio, preferences, etc.)
SUPABASE_STORAGE_BUCKET=portfolio-data

# Required Storage Buckets (must be created in Supabase Dashboard → Storage):
# - portfolio-wallpapers: macOS wallpapers (27 images + thumbnails)
# - portfolio-projects: Project screenshots and demos  
# - portfolio-assets: Profile images, icons, misc assets

# Cache duration for Supabase Storage URLs (in seconds, default: 3600 = 1 hour)
SUPABASE_STORAGE_CACHE_DURATION=3600

# =====================================================
# Supabase Edge Functions
# =====================================================

# Contact Form Submission Edge Function
# Default: {SUPABASE_URL}/functions/v1/contact-submission
SUPABASE_EDGE_FUNCTION_URL=${SUPABASE_URL}/functions/v1/contact-submission

# =====================================================
# Contact Form Configuration
# =====================================================

# Contact email address (where form submissions are sent)
CONTACT_EMAIL=hello@abdulme.link

# Rate limiting (max submissions per hour per email)
CONTACT_MAX_SUBMISSIONS_PER_HOUR=3

# =====================================================
# Analytics Configuration
# =====================================================
# Umami Analytics Website ID (already embedded in layout)
UMAMI_WEBSITE_ID=
UMAMI_SCRIPT_URL=